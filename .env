# Pointer, not configuration. This file is COMMITTED and must never hold a secret.
#
# Docker Compose reads `.env` from the directory containing docker-compose.yml —
# here — while all real configuration lives in backend/.env. Without this line
# Compose would look in the wrong file and report the master key as missing.
COMPOSE_ENV_FILES=backend/.env
